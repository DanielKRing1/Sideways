import { IconDriver } from "ssDatabase/api/types";

const Driver: IconDriver = {
    getDailyOutputLG,
    getMonthlyOutputHistogram,
    getNodeOverlapVenn,
    getDailyOutputHM,
};

export default Driver;