import React, {FC} from 'react';

import RatingMenu from './BaseRatingMenu';

type LocalRatingMenuProps = {};
const LocalRatingMenu: FC<LocalRatingMenuProps> = props => {
  return <RatingMenu />;
};

export default LocalRatingMenu;
